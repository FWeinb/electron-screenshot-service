{
  "name": "electron-screenshot-service",
  "main": "src/index.js",
  "description": "Take screenshots using electron",
  "version": "1.0.2",
  "scripts": {
    "postinstall": "npm install --prefix ./electron-screenshot",
    "test": "./node_modules/mocha/bin/mocha test"
  },
  "dependencies": {
    "axon": "^2.0.1",
    "bluebird": "^2.9.24",
    "electron-prebuilt": "^0.25.1",
    "win-spawn": "^2.0.0"
  },
  "devDependencies": {
    "image-size": "^0.3.2",
    "is-png": "^1.0.0",
    "mocha": "^2.2.4"
  }
}
